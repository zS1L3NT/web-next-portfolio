generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "cockroachdb"
    url      = env("DATABASE_URL")
}

model Project {
    title       String   @id
    description String
    tags        String[]
    updated_at  DateTime
}
